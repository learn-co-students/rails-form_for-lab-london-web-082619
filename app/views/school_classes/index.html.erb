<h2>School Classes</h2>

<% @school_classes.each do |sc| %>
<%= link_to sc.title, school_class_path(sc) %> <br>
<%= sc.room_number %> <br>
<%= link_to 'edit', edit_school_class_path(sc)%> <br> <br>
<% end %>

<%= link_to 'Create new class', new_school_class_path %>